---
title: Local Storage
---

`useLocalStorage` binds state to a `localStorage` entry. Values persist
across reloads, and changes **sync across tabs** via the `storage` event.

<switch>
  <case react>
```tsx
import { useLocalStorage } from 'kvant/react'
import * as kv from 'kvant/schema'

export function ThemeToggle() {
  const [theme, setTheme] = useLocalStorage(
    'theme',
    kv.enum(['light', 'dark']).default('light'),
  )

  return (
    <button onClick={() => setTheme(t => (t === 'light' ? 'dark' : 'light'))}>
      Theme: {theme}
    </button>
  )
}
```
  </case>
  <case vue>
```vue
<script setup lang="ts">
import { useLocalStorage } from 'kvant/vue'
import * as kv from 'kvant/schema'

const theme = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))
</script>

<template>
  <button @click="theme = theme === 'light' ? 'dark' : 'light'">
    Theme: {{ theme }}
  </button>
</template>
```
  </case>
</switch>

<include>../_shared/snippets/_interface-conventions.mdx</include>

Key maps work too:

<switch>
  <case react>
```tsx
const [prefs, setPrefs] = useLocalStorage({
  theme: kv.enum(['light', 'dark']).default('light'),
  fontSize: kv.number().default(16),
})

setPrefs(prev => ({ ...prev, fontSize: prev.fontSize + 1 }))
```
  </case>
  <case vue>
```ts
const prefs = useLocalStorage({
  theme: kv.enum(['light', 'dark']).default('light'),
  fontSize: kv.number().default(16),
})

prefs.value = { ...prefs.value, fontSize: prefs.value.fontSize + 1 }
prefs.value.fontSize += 1 // deeply reactive!
```
  </case>
</switch>

## Session storage

The same API exists for `sessionStorage` via `useSessionStorage` — scoped
to the current tab, cleared when the tab closes, and not shared across
tabs:

<switch>
  <case react>
```tsx
import { useSessionStorage } from 'kvant/react'

const [draft, setDraft] = useSessionStorage('draft', kv.string().default(''))
```
  </case>
  <case vue>
```ts
import { useSessionStorage } from 'kvant/vue'

const draft = useSessionStorage('draft', kv.string().default(''))
```
  </case>
</switch>

## Options

<AutoTypeTable
  path="node_modules/kvant/dist/index.d.mts"
  name="StorageKvantAdapterOptions"
/>

<include>../_shared/snippets/options-providers/_header.mdx</include>

<switch>
  <case react>
```tsx
import { LocalStorageOptionsProvider } from 'kvant/react'

<LocalStorageOptionsProvider defaultOptions={{ fallback: { theme: 'light' } }}>
  {children}
</LocalStorageOptionsProvider>
```
  </case>
  <case vue>
```vue
<script setup lang="ts">
import { provideLocalStorageOptions } from 'kvant/vue'

provideLocalStorageOptions({ fallback: { theme: 'light' } })
</script>
```
  </case>
</switch>
