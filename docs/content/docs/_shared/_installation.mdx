---
title: Installation
description: Install kvant with your package manager and check framework compatibility.
---

import { Step, Steps } from 'fumadocs-ui/components/steps'

<switch>
  <case react vue>
Install kvant with your package manager of choice:

```npm
npm install kvantjs
```
  </case>
  <case nuxt>
<Steps>
<Step>
Install kvant with your package manager of choice:

```npm
npm install kvantjs
```
</Step>
<Step>
Add `kvantjs` to the [`build.transpile`](https://nuxt.com/docs/api/nuxt-config#transpile)
array in your `nuxt.config.ts`:

```ts title="nuxt.config.ts"
export default defineNuxtConfig({
  build: {
    transpile: ['kvantjs'],
  },
})
```
</Step>
<Step>
You're all set!
</Step>
</Steps>
  </case>
</switch>

kvant ships as a single package with per-framework entry points:

| Entry point            | Framework                        |
| ---------------------- | -------------------------------- |
| `kvantjs/react`        | React (SPA)                      |
| `kvantjs/next`         | Next.js (app router)             |
| `kvantjs/next/pages`   | Next.js (pages router)           |
| `kvantjs/react-router` | React Router                     |
| `kvantjs/vue`          | Vue                              |
| `kvantjs/vue-router`   | Vue Router                       |
| `kvantjs/nuxt`         | Nuxt                             |
| `kvantjs/schema`       | Schema library (all frameworks)  |
| `kvantjs`              | Core adapters and types (advanced) |

Only install the peer dependency for the framework you use. The others are
optional.

## Version compatibility

| Framework    | Supported versions        |
| ------------ | ------------------------- |
| React        | [[peer:kvantjs:react]]        |
| Next.js      | [[peer:kvantjs:next]]         |
| React Router | [[peer:kvantjs:react-router]] |
| Vue          | [[peer:kvantjs:vue]]          |
| Vue Router   | [[peer:kvantjs:vue-router]]   |
| Nuxt         | [[peer:kvantjs:nuxt]]         |

<Callout type="info">
React-based entry points (`kvantjs/react`, `kvantjs/next`, `kvantjs/next/pages`,
`kvantjs/react-router`) ship with the `'use client'` directive, so they work out
of the box in React Server Components trees.
</Callout>
