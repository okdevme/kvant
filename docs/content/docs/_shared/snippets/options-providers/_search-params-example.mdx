---
title: Search Params Example
---

<switch>
  <case react>
```tsx twoslash
let children: React.ReactNode
// ---cut---
import { SearchParamsOptionsProvider } from 'kvantjs/react'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case next>
```tsx twoslash
let children: React.ReactNode
// ---cut---
import { SearchParamsOptionsProvider } from 'kvantjs/next'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case next-pages>
```tsx twoslash
let children: React.ReactNode
// ---cut---
import { RouterQueryOptionsProvider } from 'kvantjs/next/pages'

<RouterQueryOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</RouterQueryOptionsProvider>
```
  </case>
  <case react-router>
```tsx twoslash
let children: React.ReactNode
// ---cut---
import { SearchParamsOptionsProvider } from 'kvantjs/react-router'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case vue>
```vue twoslash
<script setup lang="ts">
import { provideSearchParamsOptions } from 'kvantjs/vue'

provideSearchParamsOptions({ history: 'push' })
</script>
```
  </case>
  <case vue-router>
```vue twoslash
<script setup lang="ts">
import { provideRouteQueryOptions } from 'kvantjs/vue-router'

provideRouteQueryOptions({ history: 'push' })
</script>
```
  </case>
  <case nuxt>
```vue twoslash
<script setup lang="ts">
import { provideRouteQueryOptions } from 'kvantjs/nuxt'

provideRouteQueryOptions({ history: 'push' })
</script>
```
  </case>
</switch>
