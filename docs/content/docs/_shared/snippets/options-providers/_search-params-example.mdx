---
title: Search Params Example
---

<switch>
  <case react>
```tsx
import { SearchParamsOptionsProvider } from 'kvant/react'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case next>
```tsx
import { SearchParamsOptionsProvider } from 'kvant/next'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case next-pages>
```tsx
import { RouterQueryOptionsProvider } from 'kvant/next/pages'

<RouterQueryOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</RouterQueryOptionsProvider>
```
  </case>
  <case react-router>
```tsx
import { SearchParamsOptionsProvider } from 'kvant/react-router'

<SearchParamsOptionsProvider defaultOptions={{ history: 'push' }}>
  {children}
</SearchParamsOptionsProvider>
```
  </case>
  <case vue>
```vue
<script setup lang="ts">
import { provideSearchParamsOptions } from 'kvant/vue'

provideSearchParamsOptions({ history: 'push' })
</script>
```
  </case>
  <case vue-router>
```vue
<script setup lang="ts">
import { provideRouteQueryOptions } from 'kvant/vue-router'

provideRouteQueryOptions({ history: 'push' })
</script>
```
  </case>
  <case nuxt>
```vue
<script setup lang="ts">
import { provideRouteQueryOptions } from 'kvant/nuxt'

provideRouteQueryOptions({ history: 'push' })
</script>
```
  </case>
</switch>
