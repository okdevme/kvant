---
title: Every Interface Demo
---

<switch>
  <case react>
```ts twoslash
import { useCookies, useLocalStorage, useSearchParams, useSessionStorage } from 'kvantjs/react'
import * as kv from 'kvantjs/schema'

// URL search params: shareable, bookmarkable
const [query, setQuery] = useSearchParams('q', kv.string().default(''))

// localStorage: persists across reloads, syncs across tabs
const [theme, setTheme] = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const [draft, setDraft] = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
const [consent, setConsent] = useCookies(
  'consent',
  kv.stringbool().default(false),
  { maxAge: 60 * 60 * 24 * 365 },
)
```

Full guides:
[Search Params](./search-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case next>
```ts twoslash
import { useSearchParams } from 'kvantjs/next'
import { useCookies, useLocalStorage, useSessionStorage } from 'kvantjs/react'
import * as kv from 'kvantjs/schema'

// URL search params: shareable, bookmarkable
const [query, setQuery] = useSearchParams('q', kv.string().default(''))

// localStorage: persists across reloads, syncs across tabs
const [theme, setTheme] = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const [draft, setDraft] = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
// (requires additional setup for SSR, see the full guide)
const [consent, setConsent] = useCookies(
  'consent',
  kv.stringbool().default(false),
  { maxAge: 60 * 60 * 24 * 365 },
)
```

Full guides:
[Search Params](./search-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case next-pages>
```ts twoslash
import { useRouterQuery } from 'kvantjs/next/pages'
import { useCookies, useLocalStorage, useSessionStorage } from 'kvantjs/react'
import * as kv from 'kvantjs/schema'

// Router query: shareable, bookmarkable
const [query, setQuery] = useRouterQuery('q', kv.string().default(''))

// localStorage: persists across reloads, syncs across tabs
const [theme, setTheme] = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const [draft, setDraft] = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
// (requires additional setup for SSR, see the full guide)
const [consent, setConsent] = useCookies(
  'consent',
  kv.stringbool().default(false),
  { maxAge: 60 * 60 * 24 * 365 },
)
```

Full guides:
[Router Query](./router-query) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case react-router>
```ts twoslash
import { useSearchParams } from 'kvantjs/react-router'
import { useCookies, useLocalStorage, useSessionStorage } from 'kvantjs/react'
import * as kv from 'kvantjs/schema'

// URL search params: shareable, bookmarkable
const [query, setQuery] = useSearchParams('q', kv.string().default(''))

// localStorage: persists across reloads, syncs across tabs
const [theme, setTheme] = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const [draft, setDraft] = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
// (requires additional setup for SSR, see the full guide)
const [consent, setConsent] = useCookies(
  'consent',
  kv.stringbool().default(false),
  { maxAge: 60 * 60 * 24 * 365 },
)
```

Full guides:
[Search Params](./search-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case vue>
```ts twoslash
import { useCookies, useLocalStorage, useSearchParams, useSessionStorage } from 'kvantjs/vue'
import * as kv from 'kvantjs/schema'

// URL search params: shareable, bookmarkable
const query = useSearchParams('q', kv.string().default(''))

// localStorage: persists across reloads, syncs across tabs
const theme = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const draft = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
const consent = useCookies('consent', kv.stringbool().default(false), {
  maxAge: 60 * 60 * 24 * 365,
})
```

Full guides:
[Search Params](./search-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case vue-router>
```ts twoslash
import { useRouteParams, useRouteQuery } from 'kvantjs/vue-router'
import { useCookies, useLocalStorage, useSessionStorage } from 'kvantjs/vue'
import * as kv from 'kvantjs/schema'

// Route query: shareable, bookmarkable
const query = useRouteQuery('q', kv.string().default(''))

// Route params: identifiers that belong in the path (/users/[id])
const id = useRouteParams('id', kv.int().min(1))

// localStorage: persists across reloads, syncs across tabs
const theme = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const draft = useSessionStorage('draft', kv.string().default(''))

// Cookies: readable by the server, respect Set-Cookie attributes
const consent = useCookies('consent', kv.stringbool().default(false), {
  maxAge: 60 * 60 * 24 * 365,
})
```

Full guides:
[Route Query](./route-query) ·
[Route Params](./route-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
  <case nuxt>
```ts twoslash
import { useCookies, useRouteParams, useRouteQuery } from 'kvantjs/nuxt'
import { useLocalStorage, useSessionStorage } from 'kvantjs/vue'
import * as kv from 'kvantjs/schema'

// Route query: shareable, bookmarkable
const query = useRouteQuery('q', kv.string().default(''))

// Route params: identifiers that belong in the path (/users/[id])
const id = useRouteParams('id', kv.int().min(1))

// localStorage: persists across reloads, syncs across tabs
const theme = useLocalStorage('theme', kv.enum(['light', 'dark']).default('light'))

// sessionStorage: scoped to the current tab
const draft = useSessionStorage('draft', kv.string().default(''))

// Cookies: SSR-ready out of the box (built on Nuxt's useCookie)
const locale = useCookies('locale', kv.string().default('en'))
```

Full guides:
[Route Query](./route-query) ·
[Route Params](./route-params) ·
[Local Storage](./local-storage) ·
[Cookies](./cookies)
  </case>
</switch>
