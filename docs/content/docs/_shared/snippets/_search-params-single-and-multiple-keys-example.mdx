---
title: Search Params Single and Multiple Keys Example
---

<switch>
  <case react>
```ts
const [page, setPage] = useSearchParams('page', kv.index().default(0))

const [filters, setFilters] = useSearchParams({
  q: kv.string().default(''),
  tags: kv.array(kv.string()).default([]),
  sort: kv.enum(['asc', 'desc']).default('asc'),
})
```
  </case>
  <case next-pages>
```ts
const [page, setPage] = useRouterQuery('page', kv.index().default(0))

const [filters, setFilters] = useRouterQuery({
  q: kv.string().default(''),
  tags: kv.array(kv.string()).default([]),
  sort: kv.enum(['asc', 'desc']).default('asc'),
})
```
  </case>
  <case vue>
```ts
const page = useSearchParams('page', kv.index().default(0))

const filters = useSearchParams({
  q: kv.string().default(''),
  tags: kv.array(kv.string()).default([]),
  sort: kv.enum(['asc', 'desc']).default('asc'),
})
```
  </case>
  <case vue-router nuxt>
```ts
const page = useRouteQuery('page', kv.index().default(0))

const filters = useRouteQuery({
  q: kv.string().default(''),
  tags: kv.array(kv.string()).default([]),
  sort: kv.enum(['asc', 'desc']).default('asc'),
})
```
  </case>
</switch>
